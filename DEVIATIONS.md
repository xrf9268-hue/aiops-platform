# DEVIATIONS

This document records every known difference between `aiops-platform` and the
upstream Symphony project. Three sources are jointly authoritative:

- [Symphony SPEC.md](docs/research/SPEC.md) — the protocol contract, mirrored
  verbatim from
  [upstream](https://github.com/openai/symphony/blob/main/SPEC.md) so it cannot
  drift.
- [`openai/symphony` Elixir reference implementation](https://github.com/openai/symphony/tree/main/elixir/lib/symphony_elixir) —
  the working reference. When SPEC text is ambiguous, the Elixir module's
  behavior is the tiebreaker. **Not** a porting target — see
  [`DECISION.md`](DECISION.md).
- [OpenAI Symphony announcement blog post (2026-04-27)](docs/research/2026-04-27-openai-symphony-blog.md) —
  authoritative design rationale from the authors. Mirrored in this repo so it
  cannot drift.

Practitioner accounts (advisory; not authoritative on SPEC but useful for
operational expectations and harness-engineering posture):

- [George's Symphony Electron rewrite thread (2026-05)](docs/research/2026-05-george-symphony-electron-rewrite.md) —
  first-hand operator report (50 tickets → 30 merged PRs overnight), with
  the user-visible behavior model for Rework / Cancel / Backlog transitions.
- [Addy Osmani's harness-engineering thread (2026-05)](docs/research/2026-05-addy-osmani-harness-engineering.md) —
  the framework we use to evaluate whether a component earns its keep.
  Every Reverting deviation below survives the "name the behavior it
  delivers" test described in
  [`AGENTS.md` §Harness engineering principles](AGENTS.md#harness-engineering-principles).
- [Scott Chacon's "Grit" — rewriting Git in Rust with agents (2026-06)](docs/research/2026-06-chacon-grit-rust-git.md) —
  first-hand operator report of long-running *parallel* coding agents at scale
  (500+ PRs, 7000+ commits, 99.3% of the Git test suite). The manual version of
  this orchestrator's own loop: independently validates tracker-as-coordination,
  per-task worktrees, and stateless rebuild, and sharpens where the "done" signal
  can be gamed. Advisory only — adds no component or rule (principles 2 & 6).

`aiops-platform` is a Go port of that reference. Per the project posture in
[`AGENTS.md`](AGENTS.md#spec-alignment-is-a-hard-requirement), SPEC alignment
is a hard requirement and the project is pre-release, so the cost of closing
deviations is at its minimum right now.

The umbrella tracking issue is [#67](https://github.com/xrf9268-hue/aiops-platform/issues/67).

The 2026-05-15 gap audit (PR [#82](https://github.com/xrf9268-hue/aiops-platform/pull/82), report at [`docs/audits/2026-05-15-spec-vs-go-gap-audit.md`](docs/audits/2026-05-15-spec-vs-go-gap-audit.md)) is the most recent full sweep against SPEC. It confirmed D1–D9, surfaced D10–D24, and documents 12 silent-area categories. When a row below says "see audit", treat the audit doc as the source of file:line evidence and severity reasoning.

## Deviations

| ID  | Area                                                                                              | SPEC reference                                            | Severity | Status                  | Tracking issue                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | -------- | ----------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| D1  | Agent runner protocol and session model (Codex app-server, events, timeouts, tokens)                 | §Agent Runner, §10, §7.1, §13.5, §5.3.6                   | High     | Closed                  | [#64](https://github.com/xrf9268-hue/aiops-platform/issues/64) / PR [#112](https://github.com/xrf9268-hue/aiops-platform/pull/112) |
| D2  | Orchestrator-side Linear status writes (overlaps D8; see audit)                                   | §1 boundary, §11.5                                        | Medium   | Closed                  | [#14](https://github.com/xrf9268-hue/aiops-platform/issues/14) / PR [#116](https://github.com/xrf9268-hue/aiops-platform/pull/116) |
| D3  | No multi-run reconciliation on restart (precondition: D13)                                        | §retry/reconciliation, §restart recovery                  | Medium   | Closed                  | [#68](https://github.com/xrf9268-hue/aiops-platform/issues/68)                                                                       |
| D4  | WORKFLOW.md multi-path discovery (vs single source); subset of D10                                | §workflow file                                            | Low      | Closed                  | [#72](https://github.com/xrf9268-hue/aiops-platform/issues/72) (supersedes [#69](https://github.com/xrf9268-hue/aiops-platform/issues/69)) |
| D5  | Sandbox posture relies solely on Codex CLI sandbox                                                | §safety, §harness hardening                               | Medium   | Closed                  | [#70](https://github.com/xrf9268-hue/aiops-platform/issues/70) / [#114](https://github.com/xrf9268-hue/aiops-platform/issues/114) / PR [#115](https://github.com/xrf9268-hue/aiops-platform/pull/115) |
| D6  | Postgres-backed queue (vs tracker+filesystem recovery); partial of D21                            | §restart recovery, §orchestrator runtime state            | High     | Closed                  | [#73](https://github.com/xrf9268-hue/aiops-platform/issues/73) / [#407](https://github.com/xrf9268-hue/aiops-platform/issues/407)    |
| D7  | Gitea webhook ingress (vs tracker polling)                                                        | §triggers                                                 | Medium   | Closed                  | [#74](https://github.com/xrf9268-hue/aiops-platform/issues/74)                                                                       |
| D8  | Orchestrator does PR creation / git push / Linear status writes (should be agent tools). The dead residue left behind after #76 — the `pr:` (`PRConfig`) and `safety:` (`SafetyConfig`) WORKFLOW.md config blocks (the worker no longer opens/labels PRs and never enforced `safety:`), plus the `push` / `pr_created` task-event vocabulary — was removed under #578; `pr:` / `safety:` are now rejected at load. The remaining orchestrator-side tracker-write surface — the `tracker.Transitioner` interface and the Linear client's `MoveIssueToState` / `AddComment` methods (dead with zero non-test callers since #76, but whose doc comments still described the worker-writes-tracker fallback) — was removed under #678. | §1 boundary, §tools                                       | **P0**   | Closed                  | [#76](https://github.com/xrf9268-hue/aiops-platform/issues/76) / [#578](https://github.com/xrf9268-hue/aiops-platform/issues/578) / [#678](https://github.com/xrf9268-hue/aiops-platform/issues/678)    |
| D9  | No per-tick reconciliation; in-flight runs did not stop on tracker state change (stall: see D14)  | §2.1 Goals, §8.5                                          | P1       | Closed                  | [#78](https://github.com/xrf9268-hue/aiops-platform/issues/78) / PR [#131](https://github.com/xrf9268-hue/aiops-platform/pull/131) |
| D10 | Workflow file is per-service, not per-repo-per-task (supersedes D4)                               | §5.1, §17.7                                               | High     | Closed                  | [#84](https://github.com/xrf9268-hue/aiops-platform/issues/84)                                                                       |
| D11 | Dynamic WORKFLOW.md watch/reload was unimplemented                                                | §6.2                                                      | High     | Closed                  | [#85](https://github.com/xrf9268-hue/aiops-platform/issues/85) / PR [#133](https://github.com/xrf9268-hue/aiops-platform/pull/133) |
| D12 | Workspace lifecycle hooks lacked complete ordering/cleanup semantics                              | §5.3.4, §9.4, §18.1                                       | High     | Closed                  | [#86](https://github.com/xrf9268-hue/aiops-platform/issues/86) / PR [#134](https://github.com/xrf9268-hue/aiops-platform/pull/134) / [#146](https://github.com/xrf9268-hue/aiops-platform/issues/146) |
| D13 | Workspace key by `<repo>_<task_id>` instead of sanitized issue identifier; sanitizer realigned to SPEC §4.2 in [#229](https://github.com/xrf9268-hue/aiops-platform/issues/229); reuse + `after_create` realignment in [#245](https://github.com/xrf9268-hue/aiops-platform/issues/245) | §4.2, §9.1, §9.2, §9.4, §17.2                              | High     | Closed                  | [#87](https://github.com/xrf9268-hue/aiops-platform/issues/87), [#229](https://github.com/xrf9268-hue/aiops-platform/issues/229), [#245](https://github.com/xrf9268-hue/aiops-platform/issues/245), follow-ups [#308](https://github.com/xrf9268-hue/aiops-platform/issues/308) / [#310](https://github.com/xrf9268-hue/aiops-platform/issues/310) |
| D14 | Stall detection (`stall_timeout_ms` / `turn_timeout_ms` / `read_timeout_ms`) was missing on the Codex app-server runner path | §5.3.6, §8.5 Part A                                       | Medium   | Closed                  | [#88](https://github.com/xrf9268-hue/aiops-platform/issues/88)                                                                       |
| D15 | Per-state concurrency (`max_concurrent_agents_by_state`)                                         | §5.3.5, §8.3                                              | Medium   | Closed                  | [#89](https://github.com/xrf9268-hue/aiops-platform/issues/89) / closed follow-up [#159](https://github.com/xrf9268-hue/aiops-platform/issues/159) |
| D16 | Retry used fixed delays instead of exponential backoff + 1s continuation retries                  | §7.3, §8.4, §16.6                                         | High     | Closed                  | [#90](https://github.com/xrf9268-hue/aiops-platform/issues/90) / PR [#140](https://github.com/xrf9268-hue/aiops-platform/pull/140) |
| D17 | `strings.ReplaceAll` template vs Liquid-strict; `attempt` variable never threaded                 | §5.4, §12.2, §17.1                                        | Medium   | Closed                  | [#91](https://github.com/xrf9268-hue/aiops-platform/issues/91) / [#161](https://github.com/xrf9268-hue/aiops-platform/pull/161)      |
| D18 | Issue domain model missing `priority` / `labels` / `blocked_by` / `branch_name` / timestamps      | §4.1.1                                                    | Medium   | Closed                  | [#92](https://github.com/xrf9268-hue/aiops-platform/issues/92) / [#162](https://github.com/xrf9268-hue/aiops-platform/pull/162)      |
| D19 | Candidate selection, `Todo` blocker rule, and dispatch sorting were incomplete                    | §8.2                                                      | High     | Closed                  | [#93](https://github.com/xrf9268-hue/aiops-platform/issues/93) / PR [#141](https://github.com/xrf9268-hue/aiops-platform/pull/141) |
| D20 | Linear GraphQL lacked `project.slugId` filtering, pagination, and `[ID!]` state-refresh query; narrow state refresh now feeds poll-tick reconcile | §11.2                                                     | High     | Closed                  | [#94](https://github.com/xrf9268-hue/aiops-platform/issues/94) / PR [#142](https://github.com/xrf9268-hue/aiops-platform/pull/142) / [#148](https://github.com/xrf9268-hue/aiops-platform/issues/148) / PR [#177](https://github.com/xrf9268-hue/aiops-platform/pull/177) |
| D21 | Single-source-of-truth orchestrator state absent (umbrella; D6 is partial)                        | §3.1, §4.1.8, §7.4, §13.7                                | High     | Closed                  | [#95](https://github.com/xrf9268-hue/aiops-platform/issues/95) / PR [#117](https://github.com/xrf9268-hue/aiops-platform/pull/117) / [#150](https://github.com/xrf9268-hue/aiops-platform/issues/150) |
| D22 | Run attempt phases (§7.2) and runtime events (§10.4) not modeled (depends on D1)                  | §7.2, §10.4                                               | Medium   | Closed                  | [#96](https://github.com/xrf9268-hue/aiops-platform/issues/96) / [#163](https://github.com/xrf9268-hue/aiops-platform/pull/163)      |
| D23 | `linear_graphql` client-side tool surface is wired through Codex app-server dynamic tools; SPEC §15.5 harness narrowing (mutation allow-list + audit event) implemented in [#298](https://github.com/xrf9268-hue/aiops-platform/issues/298) | §10.5, §15.5                                              | Medium   | Closed                  | [#97](https://github.com/xrf9268-hue/aiops-platform/issues/97) / PR [#116](https://github.com/xrf9268-hue/aiops-platform/pull/116) / PR [#164](https://github.com/xrf9268-hue/aiops-platform/pull/164) / [#298](https://github.com/xrf9268-hue/aiops-platform/issues/298) |
| D24 | Workflow front-matter schema: aiops keys + missing core (`hooks`, `polling`, `workspace.root`); relative `workspace.root` now resolves from the `WORKFLOW.md` directory | §5.3                                                      | Medium   | Closed                  | [#98](https://github.com/xrf9268-hue/aiops-platform/issues/98) / [#166](https://github.com/xrf9268-hue/aiops-platform/pull/166)      |
| D25 | Multi-service `WORKFLOW.md` routing schema (`services[]` + `selectRoutedCandidates` + multi-tracker fan-out) was an implementation-defined extension with no upstream equivalent — Symphony is single-project (§8.2/§11.2 select one `project.slugId`; `config/schema.ex` declares one project). Removed under #573: single-service is the only path; a stray `services:` key is logged and ignored per §5.3 forward-compat. Replaceable by running one process per service. | §5.3 extension note, §8.2, §11.2                           | Low      | Reverted                | [#143](https://github.com/xrf9268-hue/aiops-platform/issues/143) / [#16](https://github.com/xrf9268-hue/aiops-platform/issues/16) / [#573](https://github.com/xrf9268-hue/aiops-platform/issues/573) |
| D26 | Codex input-required / MCP elicitation sessions retried instead of being held as operator-visible blocked claims | §10.5, §13.7; Elixir PR [#66](https://github.com/openai/symphony/pull/66) | P1       | Closed                  | [#183](https://github.com/xrf9268-hue/aiops-platform/issues/183) / [#200](https://github.com/xrf9268-hue/aiops-platform/pull/200)    |
| D27 | Capacity-full failure-retry armed a 100 ms re-fire timer instead of rescheduling through the configured backoff with a typed "no available orchestrator slots" error | §8.4, §16.6; upstream `handle_active_retry` (orchestrator.ex:1142-1161) | Medium   | Closed                  | [#306](https://github.com/xrf9268-hue/aiops-platform/issues/306)                                                                      |
| D28 | `DefaultConfig` previously shipped `tracker.kind: gitea` instead of enforcing SPEC §6.4's REQUIRED semantics. Closed by the #244 follow-up sweep: `DefaultConfig` no longer presets the field, `validateConfig` rejects an empty kind with a SPEC-aware error, and every minimal-front-matter test fixture in `internal/workflow` / `cmd/worker` now declares `tracker.kind` explicitly. | §6.4 | Low | Closed | [#244](https://github.com/xrf9268-hue/aiops-platform/issues/244) |
| D29 | `agent.max_retry_attempts` / `agent.max_timeout_retries` caps, the `maxFailureRetries` machinery, and the `OrchestratorState.Failed` deterministic-non-retryable suppression map (+ `recordFailed` / `FinishRunNonRetryableFailed` / `ReleaseFailedIfIssueChanged` / `FailedEntry` / the `IsClaimed` Failed check / `CumulativeFailedTotal` / `MaxRecentFailed` / the `failed` + `failed_total` state-API surface) were non-SPEC harness extensions. The opt-in retry caps + `maxFailureRetries` were removed in #577; the residual deterministic-non-retryable suppression was removed in #584, leaving SPEC §8.4/§16.6 unbounded-retry-with-backoff as the only failure path. Upstream's `State` (`orchestrator.ex`) has no `failed`/give-up map — only `running`/`completed`/`claimed`/`blocked`/`retry_attempts` — and SPEC §4.1.8's runtime state and §16.6's `on_retry_timer` have no give-up branch. Upstream handles the equivalent failures (a `prompt_builder` raise / spawn error) by routing the agent-down to `schedule_issue_retry`. The #234 every-poll-tick spin the suppression guarded was itself a Go-port artifact: a deterministic task-build/worker-setup failure now rides the same §8.4 failure backoff (parked Claimed in `RetryAttempts`, so subsequent poll ticks do not re-dispatch; the retry-fire re-checks tracker eligibility and stops when the issue leaves the active set), bounding the spin to the backoff cadence as upstream does. The cap keys remain rejected at load. | §4.1.8, §8.4, §15.5, §16.6 | Low | Closed (opt-in caps removed in #577; deterministic-non-retryable suppression removed in #584) | [#215](https://github.com/xrf9268-hue/aiops-platform/issues/215) / [#577](https://github.com/xrf9268-hue/aiops-platform/issues/577) / [#584](https://github.com/xrf9268-hue/aiops-platform/issues/584) |
| D30 | Orchestrator continuation-spawn cap for legacy runners — **removed in #576**. The orchestrator used to cap clean-continuation spawns at `agent.max_turns` for runners that cannot enforce max_turns in-session (shell `claude`, mocks; the non-SPEC one-shot `codex exec` runner that originally motivated the cap was removed in [#541](https://github.com/xrf9268-hue/aiops-platform/issues/541)), flipping a still-active tracker issue to `Failed` (`continuation_budget_exhausted`) on a clean exit. That was a post-handoff terminal gate on the wrong side of SPEC §1 (same family as #557/#561): it fired after the worker exited and raced the D9 reconcile-cancel / §16.5 self-stop. At the time of #576, the port matched SPEC §7.1 by leaving continuation worker spawns unbounded and retaining `agent.max_turns` solely for the runner's in-session loop. #621 / PR #625 later reproduced that SPEC behavior as operationally defective for unfinishable active issues; #627 captured the process lesson; D34 / PR #628 records the accepted replacement hardening as a separate, current deviation rather than rewriting this historical removal. | §5.3.5, §7.1, §10.1, §15.5 | Low | Reverted/Closed (continuation-spawn cap removed in #576) | [#216](https://github.com/xrf9268-hue/aiops-platform/issues/216) / [#541](https://github.com/xrf9268-hue/aiops-platform/issues/541) / [#576](https://github.com/xrf9268-hue/aiops-platform/issues/576) |
| D31 | External-blocker cooldown entries (`.aiops/BLOCKED.json` → `retrying.kind=external_blocker`) were an aiops-platform workflow artifact extension on the wrong side of the SPEC §1 agent boundary: a worker post-turn phase consumed an agent-written artifact and the orchestrator parked the run in a bespoke `external_blocker` retry-kind cooldown. SPEC has only generic failure backoff (§8.4) and clean continuation retries (§7.1); a whole-tree grep of `openai/symphony@main` finds no structured "agent is waiting on an external dependency" artifact or per-reason cooldown. **Removed in #572** as over-design, not relocated (AGENTS.md principle 6 — upstream absence is an over-design signal; delete, don't move-to-prompt or document): the `.aiops/BLOCKED.json` reader/validator + schema, the prompt directive, the `external_blocker` retry-kind, and the `applyExternalBlocked`/`FinishRunExternalBlocked` finalize path are all gone. A still-active blocked issue now follows the SPEC default (clean continuation retry / §8.4 backoff, re-checking tracker state each cycle); the dedup-no-op-runs intent is covered by that generic backoff, and any "explain the wait" content belongs in the agent's PR/tracker comment (agent-side per §1). The #550 sandbox-startup cooldown, which had reused the same cooldown machinery, was folded into the §8.4 failure backoff in the same change — the runner still re-tags the failure to an output-free `SandboxStartupError`, and `worker --doctor` (#542) remains the preventive layer. | §1 boundary, §7.1, §8.4, §13.7, §16.6 | Medium | Reverted/Closed (cooldown artifact removed in #572) | [#468](https://github.com/xrf9268-hue/aiops-platform/issues/468) / [#572](https://github.com/xrf9268-hue/aiops-platform/issues/572) |
| D32 | Default per-turn Codex sandbox policy is derived from `codex.thread_sandbox` instead of pinned to `workspaceWrite`. Upstream `config/schema.ex` (`resolve_turn_sandbox_policy`) leaves `turn_sandbox_policy` unset and lazily defaults it to `workspaceWrite` regardless of `thread_sandbox`, so the per-turn override always re-imposes workspace-write. Combined with the aiops-only `doctor` check + Docker runbook that recommend `thread_sandbox: danger-full-access` in containers, that silently defeated the operator's thread-sandbox selection on every turn. When `turn_sandbox_policy` is unset, the loader now derives the turn policy from `thread_sandbox` (`danger-full-access`→`dangerFullAccess`, `read-only`→`readOnly`, else `workspaceWrite`); an explicit `turn_sandbox_policy` still overrides. (Upstream's `readOnlyAccess` default field is separately dropped because codex rust-v0.133 forbids it — the wire schema is the authority there.) | §6.4, §10.1 | Low | Closed (accepted deviation) | [#472](https://github.com/xrf9268-hue/aiops-platform/issues/472) / [#487](https://github.com/xrf9268-hue/aiops-platform/pull/487) |
| D33 | Worker-side post-run gates (`verify`, secret-scan, RUN_SUMMARY, policy) run as orchestrator phases on the wrong side of the SPEC §1 agent boundary (2026-05-15 audit "Deliberate extensions" items 4/5/7 + RUN_SUMMARY). Every one of them runs *after* the agent's turn returns, i.e. after the agent has already pushed (#76), so each can only flag — never prevent — and each races the D9 reconcile-cancel / §16.5 self-stop the same way `verify` did in #557 (a reconcile-cancel win skips the post-turn phase). **verify** removed in #557/#560 (surfaced to the prompt via `AppendVerifyDirective`, backstopped by PR CI). **secret-scan** removed in #561 — it scanned post-push so it could not prevent a leak, and PR CI / the host's secret scanning is the backstop; `verify.secret_scan` is rejected at load. **RUN_SUMMARY** gate removed in #561 — nothing read the artifact except the gate, the agent's PR body is the change record per SPEC §1, and the worker's own failure post-mortem was moved to `.aiops/FAILURE.md`. **That relocation is itself reverted in #575**: `.aiops/FAILURE.md` (the failure post-mortem) and `.aiops/CHANGED_FILES.txt` (the changed-file snapshot, #38-era run-artifact scaffolding) were inert, unread workspace files duplicating the SPEC §13.1/§13.2 structured event log (every write path already emits the same `ErrSummary` on `runner_end "runner failed"` / the `workspace_hook_end` error / `analysis_only_violation`), and upstream `workspace.ex` / `log_file.ex` persist no such file. Moving RUN_SUMMARY's note to `.aiops/FAILURE.md` was the delete-don't-relocate anti-pattern (principle 6); #575 deletes both files. The structured log is the source of truth for *why* a run failed; the §13.7 dashboard (a KEEP in this sweep) is the correct human-readable failure surface. **policy** path/diffstat gate (`internal/policy`, `allow_paths`/`deny_paths`/`max_changed_*`, the violation-retry loop + `policy_violation_budget`) removed in #561: SPEC §3.2 homes scope/validation rules in the operator's `WORKFLOW.md` prompt, but those rules are advisory. Repository permissions, branch protection, required review, and CI enforce what may land; neither the worker sandbox nor the coding-agent sandbox exposes a configurable repository-subpath denylist. Upstream has no such config, and the removed keys are rejected at load. **analysis_only diff gate** removed in #574 — the "four gates removed" claim missed a fifth: `policy.mode: analysis_only` still drove a worker post-turn `enforceAnalysisOnlyChanges` check (PLAN.md presence + changed-files/commits since base ref → `analysis_only_violation`) on the same wrong side of §1, racing reconcile-cancel like the rest. analysis-only is now preventive-only, delivered by the `AppendAnalysisOnlyDirective` prompt directive (SPEC §3.2); `policy.mode` (draft_pr / analysis_only) is retained purely as the selector for that directive, not as a gate. All five gates are now removed and the worker matches upstream; the RUN_SUMMARY→FAILURE.md relocation and the CHANGED_FILES.txt snapshot are also removed (#575). | §1 boundary, §13.1, §13.2 | Medium | Closed (verify + secret-scan + RUN_SUMMARY + policy + analysis_only gates removed; FAILURE.md / CHANGED_FILES.txt artifacts removed in #575) | #557 / #561 / #574 / #575 / umbrella [#67](https://github.com/xrf9268-hue/aiops-platform/issues/67) |
| D34 | Cross-session clean-continuation budget for still-active issues. SPEC §7.1 and upstream `orchestrator.ex` keep re-dispatching clean exits while the tracker issue remains active; PR #625 reproduced #621 upstream and showed this can loop forever on logically unfinishable work, burning quota and starving other issues while `blocked` stays empty. Issue #627 records the governance lesson: upstream absence can mean under-hardening only when live evidence proves the SPEC/upstream behavior is defective. The port now accepts a harness-hardening deviation implemented in PR #628: `agent.max_continuation_turns` (defaulting to `agent.max_turns`) caps cumulative clean turns across fresh and continuation dispatches. Before each eligible dispatch, the orchestrator passes the remaining clean-turn budget to the runner so a single app-server session cannot overspend the issue budget; `agent.max_turns` still caps the session and is never expanded. When the budget is exhausted, the orchestrator parks the issue in local `Blocked` with `method=continuation_budget`, records `continuation_budget_blocked`, and does not write tracker state or parse natural-language agent output. Raising the budget later does not automatically redrive existing blocked claims; an operator must change tracker state or otherwise reissue work intentionally. Failure retries remain unbounded per D29; this is only the clean still-active continuation path. | §7.1, §13.7, §16.5, §16.6 | High | Closed (accepted deviation) | [#621](https://github.com/xrf9268-hue/aiops-platform/issues/621) / [#627](https://github.com/xrf9268-hue/aiops-platform/issues/627) / PR [#625](https://github.com/xrf9268-hue/aiops-platform/pull/625) / PR [#628](https://github.com/xrf9268-hue/aiops-platform/pull/628) |
| D35 | Operator Terminal Stop. SPEC §8.5 / §16.5 and upstream `orchestrator.ex` stop active runs when tracker state leaves the active set, but they do not prevent the still-running agent from issuing an agent-owned `issueUpdate` back into an active state before the process observes cancellation; PR #625's upstream comparison reproduced this race for #622. The port now accepts a process-local safety deviation: once this worker observes the current issue in a configured terminal state during reconcile, retry cleanup, or §16.5 finalize-time self-stop without a structured agent-owned current-issue non-active handoff fact, it records an in-memory `OperatorTerminalStop` latch. Agent-owned terminal handoffs still clean the workspace and do not queue continuation, but they do not latch/suppress later intentional rework. Later dispatches and retry-fire dispatches of the same latched issue ID are suppressed even if the tracker currently reads active, cleanup-time active rechecks may skip deletion but cannot resume continuation, and the agent-visible Linear tool rejects current-issue `issueUpdate` into any configured `active_states` before HTTP dispatch. For the current issue, unsupported or ambiguous `issueUpdate` shapes are also rejected fail-closed instead of guessed; non-current issue updates keep the ordinary mutation gate behavior. The guard uses structured tracker snapshots and parsed `issueUpdate(id,input.stateId)` input only; it never parses Codex natural-language output, task-event text, or ad hoc GraphQL substrings. Agent comments/workpad writes after the stop remain possible for audit but use a separate `tool_call_mutation_post_operator_terminal_stop` event so they do not count as agent handoff activity. Like D34 blocked claims, this latch is in-process runtime state: a worker restart clears it, and recovery still comes from tracker polling plus filesystem reconciliation. The latch set is also bounded per process by `MaxRecentOperatorTerminalStops` (default 1000, #667): the oldest latch is evicted once the bound is exceeded, with `CumulativeOperatorTerminalStopsTotal` preserved for observability. Eviction is safe because an evicted issue is re-dispatched only if it currently reads active in the tracker (the poll filters terminal issues out regardless of the latch), and by the time an issue is the oldest of N distinct stops any still-running agent that could have produced the D35 re-activation race has long exited — so an active reading then is intentional rework, where dispatch is the desired outcome; recovery still comes from polling plus reconciliation. | §1 boundary, §8.5, §10.5, §13.7, §16.5 | High | Closed (accepted deviation) | [#622](https://github.com/xrf9268-hue/aiops-platform/issues/622) |
| D36 | Clone-URL credential masking (`workflow.MaskCloneURL`). An aiops-platform logging-safety utility with no direct upstream Symphony equivalent: it strips embedded basic-auth (`scheme://user:token@host`) from a clone URL before the value reaches any log, error string, or `--print-config` output (the `mirror.go` clone-failure error, `doctor_tracker.go`, `print_config.go`). It implements the SPEC §1 token-isolation posture for orchestrator-side logs — the agent never sees the token, and neither do the operator's logs. #676 added the first direct unit coverage, which exposed a fail-open gap: the helper returned the raw string whenever `url.Parse` rejected the input (a malformed port, a space in the userinfo), leaking the token into the clone-failure log. It now fails closed via a conservative `scheme://userinfo@` string strip on the `url.Parse` error path (mirroring url.Parse's last-`@`-in-authority rule, leaving an `@` in the path/query untouched). The free-text analogue `workspace.redactCredentials`/`credentialURLRe` agrees on the splitting rule. Same masking-must-not-leak class as #469/#483. | §1 boundary | Low | Closed (accepted extension) | [#676](https://github.com/xrf9268-hue/aiops-platform/issues/676) |
| D37 | Per-claim token/runtime budget guardrails (`agent.max_tokens_per_claim`, `agent.max_runtime_seconds_per_claim`). SPEC and upstream `orchestrator.ex` expose runner/runtime events but do not cap a live claim by observed token or wall-clock usage. The unattended GitHub workflow failures in #1027 showed that process-lifetime totals were too coarse for operators and that an unclear or usage-limit run could keep burning quota without an operator-visible local stop. The port accepts a narrow process-local safety deviation: configured zero values disable the guardrail; nonzero values are checked against structured runtime telemetry for the current claim; on exceedance the worker cancels the runner context, records `budget_exceeded`, parks the issue in local `Blocked` with `method=budget_exceeded`, and surfaces current-claim / ended-session / process totals in the §13.7 state API and dashboard. It does not inspect natural-language agent output, does not run PR review/merge/issue-close logic, and does not write tracker state; recovery still comes from tracker polling plus explicit operator action or process restart. | §1 boundary, §10.4, §13.7, §15.5, §16.5 | High | Closed (accepted deviation) | [#1027](https://github.com/xrf9268-hue/aiops-platform/issues/1027) |

Severity reflects the risk and the gap to SPEC, not the implementation effort.

D15 is closed because per-state caps are implemented and enforced; the active-state refresh edge case during reconcile was tracked separately in closed follow-up [#159](https://github.com/xrf9268-hue/aiops-platform/issues/159).

Status vocabulary:

- **Open**: no merged implementation has materially closed the deviation.
- **Partial**: at least one prerequisite or sub-behavior has landed, but the
  linked issue remains open because the full acceptance criteria are not met.
- **Reverting**: the current behavior is still present and is intentionally
  scheduled for removal/replacement.
- **Closed**: the linked implementation issue is closed; leave the row visible
  until umbrella #67 closes so future audits can see which D1–D24 items were
  resolved. A `Closed (accepted deviation)` qualifier means the linked issue
  closed by explicitly accepting a live divergence; keep that rationale visible.

## Deliberate extensions

There are no current standalone deliberate extensions outside the deviations
table. Accepted live divergences stay in the table as
`Closed (accepted deviation)` so audits keep their SPEC reference and acceptance
rationale. The bar is high: SPEC alignment is a hard requirement (see project
posture in [`AGENTS.md`](AGENTS.md#spec-alignment-is-a-hard-requirement)) and
cosmetic or marginal convenience does not qualify. Anything that fails the bar
moves to the deviations table above and gets a tracking issue for reversal.

> **Removed claims.** Earlier revisions of this file listed several
> "deliberate extensions" that did not survive review under the project's
> SPEC-alignment posture. They are recorded here so the correction is
> auditable:
>
> - **Gitea webhook trigger path** — value (latency, rate limit) was
>   marginal for self-hosted Gitea against minute-scale agent runs;
>   tracked as D7 and closed by #74 after replacing webhook ingress with
>   tracker polling.
> - **PostgreSQL-backed queue** — value (concurrent-worker safety) is
>   unused while the codebase assumes single-worker (#68); tracked as
>   D6 and removed under #73/#407.
> - **Multi-path WORKFLOW.md discovery** — value was cosmetic only;
>   tracked as D4, being reverted under #72.
> - **Runner abstraction supporting `mock` / `codex-app-server` / `claude`** — SPEC
>   is explicitly agent-agnostic, so this is **not a deviation at all**.
>   It was a documentation error in #69.

The 2026-05-15 audit also enumerated nine existing orchestrator-side
behaviors that look like extensions but fail the SPEC-alignment test
(verify gate, secret scan, summary gate, policy enforcement, orchestrator
Linear writes, etc.). They are catalogued in
[`docs/audits/2026-05-15-spec-vs-go-gap-audit.md` §Deliberate extensions](docs/audits/2026-05-15-spec-vs-go-gap-audit.md#deliberate-extensions);
none move to this section, all are covered by existing deviations
(notably D8, D23) or will be re-expressed as workflow extensions /
agent tools as those deviations close.

## How to use this list

If you are auditing SPEC alignment, walk the table top to bottom and check the
linked issue for the latest status. A row stays here until either:

1. The deviation is fully closed (i.e. SPEC-aligned) and the tracking issue is
   marked resolved, OR
2. The tracking issue explicitly accepts a live divergence; in that case keep
   the row here as `Closed (accepted deviation)` with the accepting PR/issue
   link.

Closing all of D1–D24 (or moving each to accepted-deviation status) is what
would let the project legitimately describe itself as a Symphony Go port
rather than "inspired by Symphony".
