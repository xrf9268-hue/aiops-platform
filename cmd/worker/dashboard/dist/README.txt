This directory is the ignored Vite build output used by Go embed.
Run `npm run build` from `cmd/worker/dashboard` before building release
worker binaries. Generated HTML, CSS, JavaScript, and font files are not
committed.
